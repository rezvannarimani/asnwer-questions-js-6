/** @format */
//add comments
// explain what this function does. you can use Google!

function whatDoIDo() {
    return Math.random() * 8 + 1;
  }
  //This is a function that multiplies a random number by eight, adds one, and then returns.

  // explain what does this function do, google it
  function whatICanDo(word1, word2) {
    return word1.concat(word2);
  }

  //This is a function that it has two parameters named word1, word2, what it returns is that it concats both parameters.